# Credentials and filled config — NEVER commit these
kantata.env
.env
.env.local
*.key
*.pem

# Agent state (only exists if someone runs the task from the repo dir)
pending_timesheet.json
data/

# OS / IDE
.DS_Store
.vscode/
.idea/
